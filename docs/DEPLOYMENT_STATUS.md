# Deployment Status

The deployment status ledger now lives at [`docs/internal/DEPLOYMENT_STATUS.md`](internal/DEPLOYMENT_STATUS.md).

Use that file as the source of truth for the current environment map. The current production hostname is `https://nyaaywatch.in`, backed by `nyaaywatch-production`. Dedicated AWS staging (`nyaaywatch-staging` / `staging.nyaaywatch.in`) is retired until the next rehearsal; see the internal ledger for DNS leftovers and re-provision notes.
